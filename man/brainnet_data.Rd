% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brainnet_data.R
\name{brainnet_data}
\alias{brainnet_data}
\title{Construct Validated Brain-Network Population Data}
\usage{
brainnet_data(populations, node_labels = NULL, communities = NULL)
}
\arguments{
\item{populations}{A named list with at least two groups. Each group is a
list containing at least two adjacency matrices.}

\item{node_labels}{Optional unique character labels for the nodes. When
omitted, consistent matrix dimnames are used if present.}

\item{communities}{Optional vector of community memberships, one per node.}
}
\value{
An object of class \code{brainnet_data}.
}
\description{
Creates the input object used by \code{\link[=brainnet_test]{brainnet_test()}}. Each observation must be
a binary, symmetric adjacency matrix with a zero diagonal, and all
observations must use the same node order. Matrix dimnames are validated
when present; otherwise matrix positions are treated as implicit labels.
}
